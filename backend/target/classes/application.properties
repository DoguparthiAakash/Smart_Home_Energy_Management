# Application Properties
spring.application.name=backend

# Database Configuration (Using H2 for testing portability, uncomment MySQL for production)
# spring.datasource.url=jdbc:mysql://localhost:3306/smarthome_db
# spring.datasource.username=root
# spring.datasource.password=password
# spring.jpa.hibernate.ddl-auto=update

# H2 Config for immediate testing
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# JWT Secret
jwt.secret=secretkey_should_be_long_and_secure_for_production_use_only_for_demo
